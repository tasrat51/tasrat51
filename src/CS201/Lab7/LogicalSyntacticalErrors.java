package CS201.Lab7;

import java.util.Scanner;

public class LogicalSyntacticalErrors {
    public static void main(String[] args) {
      // Q1.a)( it is Logical Error) it is infinite loop, so we need to decrement (i--) instead of increment.
       // variables x and a are not declared, y and b are not declared and initialized.
//       for(int i = 10; i>10; i++ ){
//          x=y;
//           a=b;
//
//            Q1.b)
//           int sum = 0;
//           Scanner scanner = new Scanner(System.in);
//            do {
//                num = scanner.nextInt();
//                sum = sum + num;
//           }until(sum>10000); // we got Syntactical, we should do while not until.
//
//            Q1.c)
//            while(x<1 && x>10) { // we have logical error, wrong condition.
//                a = b;// we have to declare and initialised variables (a and b)
            }
//
//            Q1.d)
//
//           while(a==b);// syntax error. we need to use code block({) after condition instead of semicolon;
//            {// syntax error, we should not use { here.
//                a=b; // we have to  declare and initialize variables ( a,b,x,y) before use.
//               x=y;

            }


//            {
//
//
//            }
//        }


